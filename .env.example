POSTGRES_USER=devfuel
POSTGRES_PASSWORD=devfuel
POSTGRES_DB=devfuel

SPRING_DATASOURCE_URL=jdbc:postgresql://localhost:5433/devfuel
SPRING_DATASOURCE_USERNAME=devfuel
SPRING_DATASOURCE_PASSWORD=devfuel

SERVER_PORT=8080
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173
PARSER_VERSION=v1

# OpenAI — set in your shell or a local .env that is NEVER committed.
# Do not put real secrets in this file.
OPENAI_API_KEY=sk-your-key-here
OPENAI_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com
APP_UPLOAD_DIR=data/uploads

# Leave empty in local frontend to use Vite proxy (/api → backend).
VITE_API_BASE_URL=
