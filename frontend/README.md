# DevFuel Frontend

React + Vite TypeScript skeleton for Phase 0.

## Run

```bash
npm install
npm run dev
```

`VITE_API_BASE_URL` defaults to `http://localhost:8080` (see `.env.example`).

## Scaffolding notes

- Placeholder App only — no logging UI yet
- API client + types + feature stubs are present for later wiring
